package yata

import (
	"testing"
)

func TestRead(t *testing.T) {
	// db := NewJSONDatabase(".yata")
	// tasks := make([]Task, 0)
	// err := db.Read("tasks", &tasks)
	// if err != nil {
	// 	t.Errorf(err.Error())
	// }
	// if len(tasks) == 0 {
	// 	t.Errorf("no tasks")
	// }
}
